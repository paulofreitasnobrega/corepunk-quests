import { App } from "./App.js";

const app = new App();

// document.getElementById("search-input").addEventListener('keyup', function (e) {
//     if (this.value.length > 2) {
//         app.test(this.value);
//         // document.getElementById("test").value = app.test(this.value);
//     }
// });